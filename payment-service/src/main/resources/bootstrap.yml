spring:
  cloud:
    config:
      uri: http://localhost:9196
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/auth/realms/devnation
management:
  endpoints:
    web:
      exposure:
        include: "*"

